version: 0.2

phases:
  install:
    commands:
      - apt-get update
      - apt-get install zip
  build:
    commands:
      - zip -r app.zip ./*
      - cd src
      - composer install --prefer-dist --no-interaction
artifacts:
  files:
    - app.zip